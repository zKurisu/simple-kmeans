#include "kmeans.h"

namespace kmeans {

    // Random init points
    void Kmeans::initPoints(int pointNum) {

    }
    // Random init points
    void Kmeans::initPoints(std::vector<Point> pointVec) {

    }

}
